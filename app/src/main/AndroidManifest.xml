<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.SMARTFRONTDESK">

<!--    인터넷-->
    <uses-permission android:name="android.permission.INTERNET" />
<!--    파일 읽기 & 쓰기-->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />




    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_smartfront_sft"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_smartfront_sft_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.example.SMARTFRONTDESK.MainActivity"
            android:configChanges="orientation|screenSize">
            <!-- 삼성폰 버그로 인해 넣음-->
<!--            가로모드 강제로 android:screenOrientation="landscape"-->




            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>